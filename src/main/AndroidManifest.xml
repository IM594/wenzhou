<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wenzhou">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 访问网络，进行地图相关业务数据请求，包括地图数据，路线规划，POI检索等 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 获取网络状态，根据网络状态切换进行数据请求网络转换 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 读取外置存储。如果开发者使用了so动态加载功能并且把so文件放在了外置存储区域，则需要申请该权限，否则不需要 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- 写外置存储。如果开发者使用了离线地图，并且数据写在外置存储区域，则需要申请该权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="话说温州"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoveActivity"></activity>
        <activity android:name=".SQliteActivity" />
        <activity android:name=".ListActivity" />

        <service
            android:name=".MusicService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".VActivity" />
        <activity android:name=".misuActivity" />
        <activity android:name=".mudiaoActivity" />
        <activity android:name=".kunquActivity" />
        <activity android:name=".ousuActivity" />
        <activity android:name=".guciActivity" />
        <activity android:name=".ouxiuActivity" />
        <activity android:name=".yiActivity" />
        <activity android:name=".pnjdActivity" />
        <activity android:name=".pntActivity" />
        <activity android:name=".pydsActivity" />
        <activity android:name=".pwmjActivity" />
        <activity android:name=".ptlsActivity" />
        <activity android:name=".pjxyActivity" />
        <activity android:name=".pnxjActivity" />
        <activity android:name=".PlayActivity" />
        <activity android:name=".EatActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="EcsUZlCHF6MeUDZltxZUv9c7coKy3FV7" />
    </application>

</manifest>